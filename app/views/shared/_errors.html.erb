<% if obj.errors.any? %>
  <div data-alert class='alert-box warning radius'>
    <h5>Please correct the following issue(s):</h5>
    <ul>
    <% obj.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
    <a href="#" class='close'>&times;</a>
  </div>
<% end %>
